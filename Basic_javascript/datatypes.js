const id = Symbol('123');
const anotherId = Symbol('123');
// console.log(id === anotherId);

const heroes = ["Thor", "Avenger","Spiderman"];
// console.log(heroes);
const dtls = {
    name: "Jamal",
    age: 24,
    rollNo: 20
};
console.log(dtls.rollNo);